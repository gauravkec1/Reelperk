// Example test file to verify Jest is working
describe('ReelPerk Tests', () => {
  test('should pass a basic test', () => {
    expect(1 + 1).toBe(2);
  });

  test('should verify Jest is configured correctly', () => {
    expect(true).toBeTruthy();
  });
});


